---
interface Props {
  title?: string;
  id: string;
}
const { title, id } = Astro.props;
---

<section id={id} class="flex flex-col gap-4 justify-start items-center my-8">
  <h3 class="text-2xl font-center font-bold text-gray-800 dark:text-gray-200">
    {title}
  </h3>
  <slot />
</section>
